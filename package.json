{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install",
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npm run dev --prefix apps/pocketbase\"",
    "dev:neon": "set -a; [ ! -f .env.local ] || . ./.env.local; set +a; concurrently --kill-others --raw \"npm run dev --prefix apps/web\" \"npx vercel dev --listen 3001\"",
    "build": "concurrently --raw \"npm run build --prefix apps/web\"",
    "db:setup": "set -a; . ./.env.local; set +a; node db/migrate.js && node db/seed.js",
    "start": "concurrently --kill-others --raw \"npm run start --prefix apps/pocketbase\"",
    "lint": "concurrently --raw \"npm run lint --prefix apps/web\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "knip": "^6.32.1"
  },
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@neon/config": "^1.3.0",
    "@neon/env": "^1.2.1",
    "@neondatabase/serverless": "^1.1.0"
  }
}
